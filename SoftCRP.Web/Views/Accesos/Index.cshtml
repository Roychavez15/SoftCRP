@model IEnumerable<SoftCRP.Web.Data.Entities.Acceso>

@{
    ViewData["Title"] = "Index";
}



<div class="row">
    <div class="col-md-12 col-sm-6">
        <div class="card">
            <div class="card-header" style="background-color:#002D73; color:#ffffff;">
                <h2 class="card-title"><strong>ACCESOS</strong></h2>
            </div>
            <div class="card-body">
                <table class="table table-bordered table-striped" id="table">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.User.FullName)
                            </th>
                            <th>
                                <input type="checkbox" name="select-all" value="Informacion" />
                                @Html.DisplayNameFor(model => model.Informacion)
                            </th>
                            <th>
                                <input type="checkbox" class="select-all checkbox" name="select-all" value="Analisis" />
                                @Html.DisplayNameFor(model => model.Analisis)

                            </th>
                            <th>
                                <input type="checkbox" class="select-all checkbox" name="select-all" value="Seguimiento" />
                                @Html.DisplayNameFor(model => model.Seguimiento)

                            </th>
                            <th>
                                <input type="checkbox" class="select-all checkbox" name="select-all" value="Tramites" />
                                @Html.DisplayNameFor(model => model.Tramites)
                            </th>
                            <th>
                                <input type="checkbox" class="select-all checkbox" name="select-all" value="Capacitaciones" />
                                @Html.DisplayNameFor(model => model.Capacitaciones)
                            </th>
                            <th>
                                <input type="checkbox" class="select-all checkbox" name="select-all" value="Conduccion" />
                                @Html.DisplayNameFor(model => model.Conduccion)
                            </th>
                            <th>
                                <input type="checkbox" class="select-all checkbox" name="select-all" value="Mantenimiento" />
                                @Html.DisplayNameFor(model => model.Mantenimiento)
                            </th>
                            <th>
                                <input type="checkbox" class="select-all checkbox" name="select-all" value="Siniestros" />
                                @Html.DisplayNameFor(model => model.Siniestros)
                            </th>
                            <th>
                                <input type="checkbox" class="select-all checkbox" name="select-all" value="Sustitutos" />
                                @Html.DisplayNameFor(model => model.Sustitutos)
                            </th>
                            <th>
                                <input type="checkbox" class="select-all checkbox" name="select-all" value="Graficos" />
                                @Html.DisplayNameFor(model => model.Graficos)
                            </th>
                            <th>
                                <input type="checkbox" class="select-all checkbox" name="select-all" value="GPS" />
                                @Html.DisplayNameFor(model => model.GPS)
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.User.FullName)
                            </td>
                            <td>
                                <input type="checkbox" class="select-item checkbox" name="select-item" value="Informacion-@item.Id" @(item.Informacion == true ? "checked" : "") />
                            </td>
                            <td>
                                <input type="checkbox" class="select-item checkbox" name="select-item" value="Analisis-@item.Id" @(item.Analisis == true ? "checked" : "") />
                            </td>
                            <td>
                                <input type="checkbox" class="select-item checkbox" name="select-item" value="Seguimiento-@item.Id" @(item.Seguimiento == true ? "checked" : "") />
                            </td>
                            <td>
                                <input type="checkbox" class="select-item checkbox" name="select-item" value="Tramites-@item.Id" @(item.Tramites == true ? "checked" : "") />
                            </td>
                            <td>
                                <input type="checkbox" class="select-item checkbox" name="select-item" value="Capacitaciones-@item.Id" @(item.Capacitaciones == true ? "checked" : "") />
                            </td>
                            <td>
                                <input type="checkbox" class="select-item checkbox" name="select-item" value="Conduccion-@item.Id" @(item.Conduccion == true ? "checked" : "") />
                            </td>
                            <td>
                                <input type="checkbox" class="select-item checkbox" name="select-item" value="Mantenimiento-@item.Id" @(item.Mantenimiento == true ? "checked" : "") />
                            </td>
                            <td>
                                <input type="checkbox" class="select-item checkbox" name="select-item" value="Siniestros-@item.Id" @(item.Siniestros == true ? "checked" : "") />
                            </td>
                            <td>
                                <input type="checkbox" class="select-item checkbox" name="select-item" value="Sustitutos-@item.Id" @(item.Sustitutos == true ? "checked" : "") />
                            </td>
                            <td>
                                <input type="checkbox" class="select-item checkbox" name="select-item" value="Graficos-@item.Id" @(item.Graficos == true ? "checked" : "") />
                            </td>
                            <td>
                                <input type="checkbox" class="select-item checkbox" name="select-item" value="GPS-@item.Id" @(item.GPS == true ? "checked" : "") />
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/v/dt/jszip-2.5.0/dt-1.10.20/af-2.3.4/b-1.6.0/b-colvis-1.6.0/b-flash-1.6.0/b-html5-1.6.0/b-print-1.6.0/cr-1.5.2/fc-3.3.0/fh-3.1.6/kt-2.5.1/r-2.2.3/rg-1.1.1/rr-1.2.6/sc-2.0.1/sl-1.3.1/datatables.min.css" />

    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.1.36/pdfmake.min.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.1.36/vfs_fonts.js"></script>
    <script type="text/javascript" src="https://cdn.datatables.net/v/dt/jszip-2.5.0/dt-1.10.20/af-2.3.4/b-1.6.0/b-colvis-1.6.0/b-flash-1.6.0/b-html5-1.6.0/b-print-1.6.0/cr-1.5.2/fc-3.3.0/fh-3.1.6/kt-2.5.1/r-2.2.3/rg-1.1.1/rr-1.2.6/sc-2.0.1/sl-1.3.1/datatables.min.js"></script>



    <script type="text/javascript">


        $(document).ready(function () {

            var table = $("#table").DataTable({
                autoFill: true,
                paging: true,
                lengthChange: true,
                searching: true,
                ordering: true,
                info: true,
                autoWidth: false,
                responsive: true,
                processing: true,

                language: {

                    url: "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Spanish.json"
                },

                order: [[0, 'asc']]
            });

            //new $.fn.dataTable.Buttons( table, {
            //    buttons: [
            //    'copy', 'excel', 'pdf'
            //    ]
            //});

            //table.buttons().container()
            //    .appendTo($('.col-sm-6:eq(0)', table.table().container()));


            $('input[name="select-all"]').click(function () {
                if ($(this).is(":checked")) {

                    var valor = $(this).val();
                    //alert(valor);
                    var fileData = new FormData();

                    //fileData.append("IdCita", id);
                    fileData.append("Valor", valor);
                    //var all = $("input.select-all")[1];
                    //all.checked = !all.checked
                    //var checked = all.checked;
                    //$("input.select-item").each(function (index, item) {
                    //    item.checked = checked;
                    //});
                    $.ajax({
                        type: 'POST',
                        url: '/Accesos/ActivaTodos/',
                        //url: '/SoftCRPWeb/Analisis/Crear',
                        //data: $('#PlanForm').serialize(),   //your form name.it takes all the values of model
                        data: fileData,
                        //dataType: 'json',
                        contentType: false,
                        processData: false,
                        success: function (result, textStatus, jqXHR) {

                            console.log(result);
                            if (result) {
                                
                            }
                            else {
                                //swal('No se pudo crear', 'warning');
                                return false;
                            }
                            window.location.href = '/Accesos/Index/'
                        },
                        error: function (jqXHR, textStatus, errorThrown) {

                            return false;

                        }
                    });
                }
                else if ($(this).is(":not(:checked)")) {

                    var valor = $(this).val();
                    //alert(valor);
                    var fileData = new FormData();

                    //fileData.append("IdCita", id);
                    fileData.append("Valor", valor);
                    //var all = $("input.select-all")[1];
                    //all.checked = !all.checked
                    //var checked = all.checked;
                    //$("input.select-item").each(function (index, item) {
                    //    item.checked = checked;
                    //});
                    $.ajax({
                        type: 'POST',
                        url: '/Accesos/DesactivaTodos/',
                        //url: '/SoftCRPWeb/Analisis/Crear',
                        //data: $('#PlanForm').serialize(),   //your form name.it takes all the values of model
                        data: fileData,
                        //dataType: 'json',
                        contentType: false,
                        processData: false,
                        success: function (result, textStatus, jqXHR) {

                            console.log(result);
                            if (result) {
                                
                            }
                            else {
                                //swal('No se pudo crear', 'warning');
                                return false;
                            }
                            window.location.href = '/Accesos/Index/'
                        },
                        error: function (jqXHR, textStatus, errorThrown) {

                            return false;

                        }
                    });
                }
            });

            $('input[name="select-item"]').click(function () {
                if ($(this).is(":checked")) {
                    var valor = $(this).val();
                    //alert(valor);
                    var fileData = new FormData();

                    //fileData.append("IdCita", id);
                    fileData.append("Valor", valor);
                    //var all = $("input.select-all")[1];
                    //all.checked = !all.checked
                    //var checked = all.checked;
                    //$("input.select-item").each(function (index, item) {
                    //    item.checked = checked;
                    //});
                    $.ajax({
                        type: 'POST',
                        url: '/Accesos/Activa/',
                        //url: '/SoftCRPWeb/Analisis/Crear',
                        //data: $('#PlanForm').serialize(),   //your form name.it takes all the values of model
                        data: fileData,
                        //dataType: 'json',
                        contentType: false,
                        processData: false,
                        success: function (result, textStatus, jqXHR) {

                            console.log(result);
                            if (result) {

                            }
                            else {
                                //swal('No se pudo crear', 'warning');
                                return false;
                            }
                            //window.location.href = '/Accesos/Index/'
                        },
                        error: function (jqXHR, textStatus, errorThrown) {

                            return false;

                        }
                    });
                }
                else if ($(this).is(":not(:checked)")) {
                    var valor = $(this).val();
                    //alert(valor);
                    var fileData = new FormData();

                    //fileData.append("IdCita", id);
                    fileData.append("Valor", valor);
                    //var all = $("input.select-all")[1];
                    //all.checked = !all.checked
                    //var checked = all.checked;
                    //$("input.select-item").each(function (index, item) {
                    //    item.checked = checked;
                    //});
                    $.ajax({
                        type: 'POST',
                        url: '/Accesos/Desactiva/',
                        //url: '/SoftCRPWeb/Analisis/Crear',
                        //data: $('#PlanForm').serialize(),   //your form name.it takes all the values of model
                        data: fileData,
                        //dataType: 'json',
                        contentType: false,
                        processData: false,
                        success: function (result, textStatus, jqXHR) {

                            console.log(result);
                            if (result) {

                            }
                            else {
                                //swal('No se pudo crear', 'warning');
                                return false;
                            }
                            //window.location.href = '/Accesos/Index/'
                        },
                        error: function (jqXHR, textStatus, errorThrown) {

                            return false;

                        }
                    });
                }
            });

        });
    </script>
}